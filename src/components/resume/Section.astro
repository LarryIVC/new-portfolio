---
const { title } = Astro.props
---

<section class="max-w-[700px] my-0 mx-auto mb-1 md:px-0 px-4">
  {
    title && (
      <h2 class="bg-primary text-white/90 uppercase text-center text-sm py-[2px] font-bold tracking-wide">
        {title}
      </h2>
    )
  }
  <slot />
</section>
